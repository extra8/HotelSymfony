<form method="post" action="{{ path('make_reservation_delete', {'id': make_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ make_reservation.id) }}">
    <button class="btn">Delete</button>
</form>
